{
    "name": "@pulumi/cloud-aws",
    "version": "0.6.1",
    "scripts": {
        "build": "tsc && yarn run lint",
        "lint": "tslint -c ../tslint.json -p tsconfig.json"
    },
    "devDependencies": {
        "@types/aws-sdk": "^2.7.0",
        "@types/node": "^8.0.26",
        "@types/mime": "^2.0.0",
        "tslint": "^5.7.0",
        "typescript": "^2.5.2"
    },
    "peerDependencies": {
        "@pulumi/aws": "*",
        "@pulumi/cloud": "*",
        "pulumi": "*"
    },
    "dependencies": {
        "mime": "^2.0.3"
    }
}
