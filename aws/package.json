{
    "name": "@pulumi/cloud-aws",
    "version": "0.6.1",
    "scripts": {
        "build": "tsc && yarn run lint",
        "lint": "tslint -c ../tslint.json -p tsconfig.json"
    },
    "devDependencies": {
        "@types/node": "^8.0.26",
        "tslint": "^5.7.0",
        "typescript": "^2.5.2"
    },
    "peerDependencies": {
        "@pulumi/aws": "*",
        "@pulumi/cloud": "*",
        "pulumi": "*"
    },
    "dependencies": {
        "@types/aws-sdk": "^2.7.0"
    }
}
